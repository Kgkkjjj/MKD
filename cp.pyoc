import sys
from fs import copy_file


def main(argv):
    """Copy a file."""
    if len(argv) < 3:
        print(f"Usage: {argv[0]} <src> <dst>")
        return
    copy_file(argv[1], argv[2])


if __name__ == '__main__':
    main(sys.argv)
