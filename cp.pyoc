import sys
from fs import copy_file
from cli import require_args

USAGE = "Usage: cp.pyoc <src> <dst>"


def main(argv):
    """Copy a file."""
    if not require_args(argv, 2, USAGE):
        return
    copy_file(argv[1], argv[2])


if __name__ == '__main__':
    main(sys.argv)
