import sys
import getpass
from cli import require_args

USAGE = "Usage: whoami.pyoc"


def main(argv):
    """Print the current user name."""
    if not require_args(argv, 0, USAGE):
        return
    print(getpass.getuser())


if __name__ == '__main__':
    main(sys.argv)
