import sys
import os
from cli import require_args

USAGE = "Usage: env.pyoc <VAR>"


def main(argv):
    """Print the value of an environment variable."""
    if not require_args(argv, 1, USAGE):
        return
    print(os.environ.get(argv[1], ''))


if __name__ == '__main__':
    main(sys.argv)
