import sys
from file_utils import move
from cli import require_args

USAGE = "Usage: mv.pyoc <src> <dst>"


def main(argv):
    """Move or rename a file."""
    if not require_args(argv, 2, USAGE):
        return
    move(argv[1], argv[2])


if __name__ == '__main__':
    main(sys.argv)
