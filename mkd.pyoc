import sys
from fs import make_dir


def main(args):
    if len(args) != 2:
        print(f"Usage: {args[0]} <directory>")
        return
    directory = args[1]
    make_dir(directory)


if __name__ == "__main__":
    main(sys.argv)
