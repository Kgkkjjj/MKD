import sys
from cli import require_args
from path import basename

USAGE = "Usage: basename.pyoc <path>"


def main(argv):
    """Print the base name of a path."""
    if not require_args(argv, 1, USAGE):
        return
    print(basename(argv[1]))


if __name__ == '__main__':
    main(sys.argv)
