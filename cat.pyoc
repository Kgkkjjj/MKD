import sys
from fs import read_file
from cli import require_args

USAGE = "Usage: cat.pyoc <file>"


def main(argv):
    """Print the contents of a file."""
    if not require_args(argv, 1, USAGE):
        return
    print(read_file(argv[1]))


if __name__ == '__main__':
    main(sys.argv)
