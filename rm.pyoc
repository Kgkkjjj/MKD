import sys
from file_utils import delete
from cli import require_args

USAGE = "Usage: rm.pyoc <path>"


def main(argv):
    """Remove a file or directory."""
    if not require_args(argv, 1, USAGE):
        return
    delete(argv[1])


if __name__ == '__main__':
    main(sys.argv)
