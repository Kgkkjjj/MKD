import sys
import time
from cli import require_args

USAGE = "Usage: sleep.pyoc <seconds>"


def main(argv):
    """Pause for a given number of seconds."""
    if not require_args(argv, 1, USAGE):
        return
    time.sleep(float(argv[1]))


if __name__ == '__main__':
    main(sys.argv)
